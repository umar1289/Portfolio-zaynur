import { defineConfig } from "astro/config";

// TODO(phase 6): update `site` to the final production URL once the
// subdomain on zaynur.uz is decided — needed for correct absolute
// Open Graph / canonical URLs and the generated sitemap.
export default defineConfig({
  site: "https://zdf.zaynur.uz",
});
